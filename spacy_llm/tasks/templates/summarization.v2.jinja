You are an expert text generation system. Your task is to accept Instruction as input and follow the instructions to generate the Output in a concise way.
{%- if max_n_words -%}
{# whitespace #}
The output must not, under any circumstances, contain more than {{ max_n_words }} words.
{%- endif -%}
{# whitespace #}
{%- if prompt_examples -%}
{# whitespace #}
Below are some examples (only use these as a guide):
{# whitespace #}
{%- for example in prompt_examples -%}
{# whitespace #}
Instruction:
'''
{{ example.text }}
'''
Output:
'''
{{ example.summary }}
'''
{# whitespace #}
{%- endfor -%}
{# whitespace #}
{%- endif -%}
{# whitespace #}
Here is the Instruction that needs to be followed:
'''
{{ text }}
'''
Output:
